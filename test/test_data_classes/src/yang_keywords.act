import base64
import json
import std.xml as xml
import yang.adata as yadata
import yang.gdata as ygdata
import yang.xml as yxml
import yang.xpath as yxpath
from yang.identityref import Identityref, PartialIdentityref
from yang.pattern import YangPattern
from yang.schema import *
from yang.type import Decimal, Ranges

# == This file is generated ==




_identities: list[DIdentity] = []


SRC_DNODE_CHILD_0: DContainer = DContainer(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='class', config=True, presence=False, children=[
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='actor', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='extension', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='from', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='import', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='protocol', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='type', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))
])

SRC_DNODE_CHILD_1: DList = DList(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='import', key=[
'from',
'type'
    ], config=True, min_elements=0, ordered_by='system', children=[
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='from', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='type', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
    DLeaf(module='keywords', namespace='http://example.com/keywords', prefix='kw', name='class', config=True, mandatory=False, type=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))
])

SRC_DNODE: DRoot = DRoot(
    children=[SRC_DNODE_CHILD_0, SRC_DNODE_CHILD_1],
    identities=[],
    rpcs=[],
    module_metadata={'http://example.com/keywords':('keywords', None)},
)

SRC_YANG_0: str = r"""module keywords {
    yang-version "1.1";
    namespace "http://example.com/keywords";
    prefix "kw";
    container class {
        leaf actor {
            type string;
        }
        leaf extension {
            type string;
        }
        leaf from {
            type string;
        }
        leaf import {
            type string;
        }
        leaf protocol {
            type string;
        }
        leaf type {
            type string;
        }
    }
    list import {
        key "from type";
        leaf from {
            type string;
        }
        leaf type {
            type string;
        }
        leaf class {
            type string;
        }
    }
}"""

pure def src_yang() -> list[str]:
    return [
        SRC_YANG_0,
    ]


NS_keywords: str = 'http://example.com/keywords'


_breaker1: None = None
class keywords__class(yadata.MNode):
    actor: ?str
    extension: ?str
    from: ?str
    import: ?str
    protocol: ?str
    type: ?str

    mut def __init__(self, actor: ?str, extension: ?str, from: ?str, import: ?str, protocol: ?str, type: ?str) -> None:
        self.actor = actor
        self.extension = extension
        self.from = from
        self.import = import
        self.protocol = protocol
        self.type = type

    mut def to_gdata(self) -> ygdata.Node:
        return yadata.from_adata(SRC_DNODE, self, loose=False, root_path=['keywords:class'])

    @staticmethod
    mut def from_gdata(n: ?ygdata.Node) -> keywords__class:
        if n is not None:
            return keywords__class(actor=n.get_opt_str(ygdata.Id(NS_keywords, 'actor')), extension=n.get_opt_str(ygdata.Id(NS_keywords, 'extension')), from=n.get_opt_str(ygdata.Id(NS_keywords, 'from')), import=n.get_opt_str(ygdata.Id(NS_keywords, 'import')), protocol=n.get_opt_str(ygdata.Id(NS_keywords, 'protocol')), type=n.get_opt_str(ygdata.Id(NS_keywords, 'type')))
        return keywords__class()

    mut def copy(self) -> keywords__class:
        """Create a deep copy of this adata object"""
        return keywords__class.from_gdata(self.to_gdata())


_breaker2: None = None
class keywords__import_entry(yadata.MNode):
    from: str
    type: str
    class: ?str

    mut def __init__(self, from: str, type: str, class: ?str) -> None:
        self.from = from
        self.type = type
        self.class = class

    mut def to_gdata(self) -> ygdata.Node:
        return yadata.from_adata(SRC_DNODE, self, loose=False, root_path=['keywords:import'])

    @staticmethod
    mut def from_gdata(n: ygdata.Node) -> keywords__import_entry:
        return keywords__import_entry(from=n.get_str(ygdata.Id(NS_keywords, 'from')), type=n.get_str(ygdata.Id(NS_keywords, 'type')), class=n.get_opt_str(ygdata.Id(NS_keywords, 'class')))

    mut def copy(self) -> keywords__import_entry:
        """Create a deep copy of this adata object"""
        return keywords__import_entry.from_gdata(self.to_gdata())

_breaker3: None = None
class keywords__import(yadata.MKeyedList[(from: str, type: str), keywords__import_entry]):
    mut def __init__(self, elements: list[keywords__import_entry]=[]) -> None:
        yadata.MKeyedList.__init__(self, lambda e: (from=e.from, type=e.type), elements)

    mut def create(self, key: (from: str, type: str), class: ?str=None) -> keywords__import_entry:
        e = self.get(key)
        if e is not None:
            if class is not None:
                e.class = class
            return e
        (from=from, type=type) = key
        res = keywords__import_entry(from=from, type=type, class=class)
        self._append(res)
        return res

    @staticmethod
    mut def from_gdata(n: ?ygdata.List) -> list[keywords__import_entry]:
        if n is not None:
            return [keywords__import_entry.from_gdata(e) for e in n.elements.values()]
        return []

    mut def copy(self) -> keywords__import:
        """Create a deep copy of this list object"""
        # Copy each element in the list
        copied_elements = []
        for e in self:
            ce = e.copy()
            if ce is not None:
                copied_elements.append(ce)
        return keywords__import(elements=copied_elements)


_breaker4: None = None
class root(yadata.MNode):
    class: keywords__class
    import: keywords__import

    mut def __init__(self, class: ?keywords__class=None, import: list[keywords__import_entry]=[]) -> None:
        self.class = class if class is not None else keywords__class()
        self.import = keywords__import(elements=import)

    mut def to_gdata(self) -> ygdata.Node:
        return yadata.from_adata(SRC_DNODE, self, loose=False)

    @staticmethod
    mut def from_gdata(n: ?ygdata.Node) -> root:
        if n is not None:
            return root(class=keywords__class.from_gdata(n.get_opt_cnt(ygdata.Id(NS_keywords, 'class'))), import=keywords__import.from_gdata(n.get_opt_list(ygdata.Id(NS_keywords, 'import'))))
        return root()

    mut def copy(self) -> root:
        """Create a deep copy of this adata object"""
        return root.from_gdata(self.to_gdata())


actor rpc_root(tp: ygdata.TreeProvider):
    pass

mut def src_schema() -> dict[str, SchemaNode]:
    res = {}
    res["keywords"] = Module('keywords', yang_version=1.1, namespace='http://example.com/keywords', prefix='kw', children=[
    Container('class', children=[
        Leaf('actor', type=Type('string')),
        Leaf('extension', type=Type('string')),
        Leaf('from', type=Type('string')),
        Leaf('import', type=Type('string')),
        Leaf('protocol', type=Type('string')),
        Leaf('type', type=Type('string'))
    ]),
    List('import', key='from type', children=[
        Leaf('from', type=Type('string')),
        Leaf('type', type=Type('string')),
        Leaf('class', type=Type('string'))
    ])
])
    return res

