name: Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actonlang/setup-acton@v1
        with:
          channel: 'tip'
      - name: "Check out repository code"
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: "Cache acton stuff"
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/acton
            ~/.cache/ayang
            out/
            !out/bin
          # Hacky cache update:
          # https://github.com/actions/cache/blob/main/tips-and-workarounds.md#update-a-cache
          key: acton-${{ github.run_id }}
          restore-keys: |
            acton
      - run: |
          make gen
          git diff --exit-code
      - run: acton build
      - run: acton test
      - run: acton test perf
      - run: make test-daclass
      - run: make test-data-source-roundtrip
      # another diff check because test-daclass also generates code
      - run: git diff --exit-code
      - run: make test-yang-compile

  test-stratoweave:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actonlang/setup-acton@v1
        with:
          channel: 'tip'
      - name: "Check out StratoWeave repository code"
        uses: actions/checkout@v4
        with:
          repository: stratoweave/stratoweave
      - name: "Update yang library dependency"
      # We use the commit SHA for both the main stratoweave app and gen_dmc, to ensure we're getting the same Zig package hash in both
        run: |
          acton pkg add yang --url https://github.com/stratoweave/acton-yang/archive/${{ github.sha }}.zip
          cd gen_dmc
          acton pkg add yang --url https://github.com/stratoweave/acton-yang/archive/${{ github.sha }}.zip
      - name: "Regenerate StratoWeave adata"
        run: |
          make
          git diff
      - name: "Run StratoWeave tests"
        run: |
          acton test
          acton test perf
