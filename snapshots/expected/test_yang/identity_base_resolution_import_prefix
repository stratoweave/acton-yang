import base64
import json
import xml
import yang
import yang.adata
import yang.gdata
import yang.xml
import yang.xpath
from yang.identityref import Identityref, PartialIdentityref
from yang.pattern import YangPattern
from yang.schema import *
from yang.type import Decimal, Ranges

# == This file is generated ==




_base_base_module_PROTOCOL = DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='PROTOCOL', base=[])
_base_base_module_TCP = DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='TCP', base=[_base_base_module_PROTOCOL])
_base_derived_module_HTTP = DIdentity(module='derived-module', namespace='http://example.com/derived', prefix='derived', name='HTTP', base=[_base_base_module_TCP])
_base_derived_module_HTTPS = DIdentity(module='derived-module', namespace='http://example.com/derived', prefix='derived', name='HTTPS', base=[_base_derived_module_HTTP])
_identities = [
    _base_base_module_PROTOCOL,
    _base_base_module_TCP,
    _base_derived_module_HTTP,
    _base_derived_module_HTTPS,
]


# Identityref constants
base_module_PROTOCOL = Identityref('PROTOCOL', ns='http://example.com/base', mod='base-module', pfx='base-pfx')
base_module_TCP = Identityref('TCP', ns='http://example.com/base', mod='base-module', pfx='base-pfx')
derived_module_HTTP = Identityref('HTTP', ns='http://example.com/derived', mod='derived-module', pfx='derived')
derived_module_HTTPS = Identityref('HTTPS', ns='http://example.com/derived', mod='derived-module', pfx='derived')


SRC_DNODE_IDENTITY_0 = DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='PROTOCOL')

SRC_DNODE_IDENTITY_1 = DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='TCP', base=[
        DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='PROTOCOL')
    ])

SRC_DNODE_IDENTITY_2 = DIdentity(module='derived-module', namespace='http://example.com/derived', prefix='derived', name='HTTP', base=[
        DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='TCP', base=[
                DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='PROTOCOL')
            ])
    ])

SRC_DNODE_IDENTITY_3 = DIdentity(module='derived-module', namespace='http://example.com/derived', prefix='derived', name='HTTPS', base=[
        DIdentity(module='derived-module', namespace='http://example.com/derived', prefix='derived', name='HTTP', base=[
                DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='TCP', base=[
                        DIdentity(module='base-module', namespace='http://example.com/base', prefix='base-pfx', name='PROTOCOL')
                    ])
            ])
    ])

SRC_DNODE = DRoot(
    children=[],
    identities=[SRC_DNODE_IDENTITY_0, SRC_DNODE_IDENTITY_1, SRC_DNODE_IDENTITY_2, SRC_DNODE_IDENTITY_3],
    rpcs=[],
    module_metadata={'http://example.com/base':('base-module', None), 'http://example.com/derived':('derived-module', None)},
)

NS_base_module = 'http://example.com/base'
NS_derived_module = 'http://example.com/derived'


_breaker1 = None
class root(yang.adata.MNode):

    mut def __init__(self) -> None:
        self._ns = ''
        pass

    mut def to_gdata(self) -> yang.gdata.Node:
        return yang.adata.from_adata(SRC_DNODE, self, loose=False)

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> root:
        if n is not None:
            return root()
        return root()

    def copy(self) -> root:
        """Create a deep copy of this adata object"""
        return root.from_gdata(self.to_gdata())


actor rpc_root(tp: yang.gdata.TreeProvider):
    pass

