import base64
import json
import std.xml as xml
import yang.adata as yadata
import yang.gdata as ygdata
import yang.xml as yxml
import yang.xpath as yxpath
from yang.identityref import Identityref, PartialIdentityref
from yang.pattern import YangPattern
from yang.schema import *
from yang.type import Decimal, Ranges

# == This file is generated ==




_identities: list[DIdentity] = []


SRC_DNODE_CHILD_0: DList = DList(module='foo', namespace='http://example.com/foo', prefix='foo', name='li1', key=['l1'], config=True, min_elements=1, ordered_by='system', children=[
    DLeaf(module='foo', namespace='http://example.com/foo', prefix='foo', name='l1', config=True, mandatory=False, type_=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))
])

SRC_DNODE_CHILD_1: DLeafList = DLeafList(module='foo', namespace='http://example.com/foo', prefix='foo', name='ll1', config=True, min_elements=1, ordered_by='system', type_=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))

SRC_DNODE: DRoot = DRoot(
    children=[SRC_DNODE_CHILD_0, SRC_DNODE_CHILD_1],
    identities=[],
    rpcs=[],
    module_metadata={'http://example.com/foo':('foo', None)},
)

NS_foo: str = 'http://example.com/foo'


_breaker1: None = None
class foo__li1_entry(yadata.MNode):
    l1: str

    mut def __init__(self, l1: str) -> None:
        self.l1 = l1

    mut def to_gdata(self) -> ygdata.Node:
        return yadata.from_adata(SRC_DNODE, self, loose=False, root_path=['foo:li1'])

    @staticmethod
    mut def from_gdata(n: ygdata.Node) -> foo__li1_entry:
        return foo__li1_entry(l1=n.get_str(ygdata.Id(NS_foo, 'l1')))

    mut def copy(self) -> foo__li1_entry:
        """Create a deep copy of this adata object"""
        return foo__li1_entry.from_gdata(self.to_gdata())

_breaker2: None = None
class foo__li1(yadata.MKeyedList[str, foo__li1_entry]):
    mut def __init__(self, elements: list[foo__li1_entry]=[]) -> None:
        yadata.MKeyedList.__init__(self, lambda e: e.l1, elements)

    mut def create(self, key: str) -> foo__li1_entry:
        e = self.get(key)
        if e is not None:
            return e
        l1 = key
        res = foo__li1_entry(l1=l1)
        self.elements.append(res)
        return res

    @staticmethod
    mut def from_gdata(n: ?ygdata.List) -> list[foo__li1_entry]:
        if n is not None:
            return [foo__li1_entry.from_gdata(e) for e in n.elements]
        return []

    mut def copy(self) -> foo__li1:
        """Create a deep copy of this list object"""
        # Copy each element in the list
        copied_elements = []
        for e in self:
            ce = e.copy()
            if ce is not None:
                copied_elements.append(ce)
        return foo__li1(elements=copied_elements)


_breaker3: None = None
class root(yadata.MNode):
    li1: foo__li1
    ll1: list[str]

    mut def __init__(self, li1: list[foo__li1_entry]=[], ll1: ?list[str]=None) -> None:
        self.li1 = foo__li1(elements=li1)
        self.ll1 = ll1 if ll1 is not None else []

    mut def to_gdata(self) -> ygdata.Node:
        return yadata.from_adata(SRC_DNODE, self, loose=False)

    @staticmethod
    mut def from_gdata(n: ?ygdata.Node) -> root:
        if n is not None:
            return root(li1=foo__li1.from_gdata(n.get_list(ygdata.Id(NS_foo, 'li1'))), ll1=n.get_strs(ygdata.Id(NS_foo, 'll1')))
        raise ValueError('Missing required subtree root')

    mut def copy(self) -> root:
        """Create a deep copy of this adata object"""
        return root.from_gdata(self.to_gdata())


actor rpc_root(tp: ygdata.TreeProvider):
    pass

