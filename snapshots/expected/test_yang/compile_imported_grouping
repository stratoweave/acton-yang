import base64
import json
import xml
import yang
import yang.adata
import yang.gdata
import yang.xml
import yang.xpath
from yang.identityref import Identityref, PartialIdentityref
from yang.pattern import YangPattern
from yang.schema import *
from yang.type import Decimal, Ranges

# == This file is generated ==




_base_foo_id1 = DIdentity(module='foo', namespace='http://example.com/foo', prefix='foo', name='id1', base=[])
_base_foo_id2 = DIdentity(module='foo', namespace='http://example.com/foo', prefix='foo', name='id2', base=[_base_foo_id1])
_identities = [
    _base_foo_id1,
    _base_foo_id2,
]


# Identityref constants
foo_id1 = Identityref('id1', ns='http://example.com/foo', mod='foo', pfx='foo')
foo_id2 = Identityref('id2', ns='http://example.com/foo', mod='foo', pfx='foo')


SRC_DNODE_CHILD_0 = DContainer(module='bar', namespace='http://example.com/bar', prefix='bar', name='c1', config=True, presence=False, children=[
    DList(module='bar', namespace='http://example.com/bar', prefix='bar', name='li1', key=['l1'], config=True, min_elements=0, ordered_by='system', children=[
        DLeaf(module='bar', namespace='http://example.com/bar', prefix='bar', name='l1', config=True, description='leaf 1', mandatory=False, type_=DTypeString(name='foo:string2', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[])),
        DLeaf(module='bar', namespace='http://example.com/bar', prefix='bar', name='l2', config=True, mandatory=False, type_=DTypeIdentityref(name='identityref', description=None, reference=None, exts=[], builtin_type='identityref', default=None, identity_bases=[_base_foo_id1], identities=_identities))
    ])
])

SRC_DNODE_IDENTITY_0 = DIdentity(module='foo', namespace='http://example.com/foo', prefix='foo', name='id1')

SRC_DNODE_IDENTITY_1 = DIdentity(module='foo', namespace='http://example.com/foo', prefix='foo', name='id2', base=[
        DIdentity(module='foo', namespace='http://example.com/foo', prefix='foo', name='id1')
    ])

SRC_DNODE = DRoot(
    children=[SRC_DNODE_CHILD_0],
    identities=[SRC_DNODE_IDENTITY_0, SRC_DNODE_IDENTITY_1],
    rpcs=[],
    module_metadata={'http://example.com/foo':('foo', None), 'http://example.com/bar':('bar', None)},
)

NS_bar = 'http://example.com/bar'
NS_foo = 'http://example.com/foo'


_breaker1 = None
class bar__c1__li1_entry(yang.adata.MNode):
    l1: str
    l2: ?Identityref

    mut def __init__(self, l1: str, l2: ?Identityref) -> None:
        self._ns = 'http://example.com/bar'
        self.l1 = l1
        self.l2 = l2

    def _get_attr(self, name: str) -> ?value:
        if name == 'l1':
            return self.l1
        if name == 'l2':
            return self.l2

    mut def to_gdata(self) -> yang.gdata.Node:
        return yang.adata.from_adata(SRC_DNODE, self, loose=False, root_path=['bar:c1', 'li1'])

    @staticmethod
    mut def from_gdata(n: yang.gdata.Node) -> bar__c1__li1_entry:
        return bar__c1__li1_entry(l1=n.get_str(yang.gdata.Id(NS_bar, 'l1')), l2=n.get_opt_Identityref(yang.gdata.Id(NS_bar, 'l2')))

    def copy(self) -> bar__c1__li1_entry:
        """Create a deep copy of this adata object"""
        return bar__c1__li1_entry.from_gdata(self.to_gdata())

_breaker2 = None
class bar__c1__li1(yang.adata.MNode):
    elements: list[bar__c1__li1_entry]
    mut def __init__(self, elements: list[bar__c1__li1_entry]=[]) -> None:
        self._ns = 'http://example.com/bar'
        self._name = 'li1'
        self.elements = elements

    mut def create(self, l1: str, l2: ?Identityref=None) -> bar__c1__li1_entry:
        for e in self:
            match = True
            if e.l1 != l1:
                match = False
                continue
            if match:
                if l2 is not None:
                    e.l2 = l2
                return e

        res = bar__c1__li1_entry(l1=l1, l2=l2)
        self.elements.append(res)
        return res

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.List) -> list[bar__c1__li1_entry]:
        if n is not None:
            return [bar__c1__li1_entry.from_gdata(e) for e in n.elements]
        return []

    def copy(self) -> bar__c1__li1:
        """Create a deep copy of this list object"""
        # Copy each element in the list
        copied_elements = []
        for e in self:
            ce = e.copy()
            if ce is not None:
                copied_elements.append(ce)
        return bar__c1__li1(elements=copied_elements)

extension bar__c1__li1(Iterable[bar__c1__li1_entry]):
    def __iter__(self) -> Iterator[bar__c1__li1_entry]:
        return self.elements.__iter__()

_breaker3 = None
class bar__c1(yang.adata.MNode):
    li1: bar__c1__li1

    mut def __init__(self, li1: list[bar__c1__li1_entry]=[]) -> None:
        self._ns = 'http://example.com/bar'
        self.li1 = bar__c1__li1(elements=li1)

    def _get_attr(self, name: str) -> ?value:
        if name == 'li1':
            return iter(self.li1)

    mut def to_gdata(self) -> yang.gdata.Node:
        return yang.adata.from_adata(SRC_DNODE, self, loose=False, root_path=['bar:c1'])

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> bar__c1:
        if n is not None:
            return bar__c1(li1=bar__c1__li1.from_gdata(n.get_opt_list(yang.gdata.Id(NS_bar, 'li1'))))
        return bar__c1()

    def copy(self) -> bar__c1:
        """Create a deep copy of this adata object"""
        return bar__c1.from_gdata(self.to_gdata())


_breaker4 = None
class root(yang.adata.MNode):
    c1: bar__c1

    mut def __init__(self, c1: ?bar__c1=None) -> None:
        self._ns = ''
        self.c1 = c1 if c1 is not None else bar__c1()

    def _get_attr(self, name: str) -> ?value:
        if name == 'c1':
            return self.c1

    mut def to_gdata(self) -> yang.gdata.Node:
        return yang.adata.from_adata(SRC_DNODE, self, loose=False)

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> root:
        if n is not None:
            return root(c1=bar__c1.from_gdata(n.get_opt_cnt(yang.gdata.Id(NS_bar, 'c1'))))
        return root()

    def copy(self) -> root:
        """Create a deep copy of this adata object"""
        return root.from_gdata(self.to_gdata())


actor rpc_root(tp: yang.gdata.TreeProvider):
    pass

